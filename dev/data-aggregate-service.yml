server:
  port: ${PORT:8021}
  
route-config:
  routes:
    -
      objectName: USER_OBJECT
      serviceName: elearning-service
      endPoint: /elearning/users/{id}
      method: GET
      description: Return user info by user id
