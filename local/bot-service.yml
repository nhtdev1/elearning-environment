server:
  port: ${PORT:8086}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/elearning_dev
    username: postgres
    password: X@x123987654
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: bot-service-dev-pool
      maximum-pool-size: 20
      max-lifetime: 1800000
      idle-timeout: 300000
      connection-timeout: 30000
      minimum-idle: 2
  data:
    redis:
      database: 0
      port: 6379
      timeout: 30000
      host: localhost
      username: default
      password: 12345678x@X

bot:
  service-api-key: ${BOT_SERVICE_API_KEY:bot-internal-secret-key}
  elearning-base-url: ${ELEARNING_BASE_URL:http://localhost:7716/elearning}
  elearning-ws-url: ${ELEARNING_WS_URL:ws://localhost:7716/elearning/ws}
